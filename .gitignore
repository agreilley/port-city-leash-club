functions/node_modules/
.DS_Store
.claude/launch.json

# Admin-only, writes demo data via firestore.rules as the signed-in admin —
# same reason the original seed pages were deleted in cbafc7e. Never commit.
dev/seed-demo.html

# Generated by dev/render-emails.js — regenerate any time, never commit.
dev/email-previews/

# Throwaway member-welcome send test — edit TEST_EMAIL locally, never commit.
dev/test-member-welcome-setup.js
dev/test-member-welcome-teardown.js
dev/.test-member-welcome-uid
