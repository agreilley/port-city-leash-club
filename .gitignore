functions/node_modules/
.DS_Store
.claude/launch.json

# Admin-only, writes demo data via firestore.rules as the signed-in admin —
# same reason the original seed pages were deleted in cbafc7e. Never commit.
dev/seed-demo.html
